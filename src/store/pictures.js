const initialState = {

};

const picturesReducer = function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};

export default picturesReducer;